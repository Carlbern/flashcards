Currently "working" decently, but some features missing

To use the programme currently, you clone the repository and then run the file "flashcards.py" as follows:
  ```console
  python3 flashcards.py
  ```
