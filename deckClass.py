import cardClass
import writeToFile

#Define class Deck
class Deck:
    def __init__(self, name, card):
        self.name = name
        self.cards = cards
#Creates cards and deck
cards = cardClass.cards
deck = Deck("test", cards)

